# Tasks: Restructure to Nx Monorepo with Tauri v2 Desktop App

## Phase 1: Infrastructure (Foundation)

*Nx workspace, directory structure, project configuration*

### 1.1 Nx Workspace Setup

- [x] 1.1.1 Initialize Nx workspace with `npx create-nx-workspace@latest logictales --preset=apps`
- [x] 1.1.2 Install required plugins: `@nx/vite`, `@nx/react`, `@nx/node`, `@nx/js`
- [x] 1.1.3 Configure root `nx.json` with build caching and task pipelines
- [x] 1.1.4 Create `pnpm-workspace.yaml` for package management

### 1.2 Directory Restructure

- [x] 1.2.1 Create `apps/backend`, `apps/game`, `apps/app`, `tools/love-builder` directories
- [x] 1.2.2 Move `backend/src/*` to `apps/backend/src/`
- [x] 1.2.3 Move `backend/package.json` and `backend/tsconfig.json` to `apps/backend/`
- [x] 1.2.4 Move root Lua files (`main.lua`, `conf.lua`, `gamestate.lua`, `logic.lua`, `renderer.lua`, `editor.lua`, `schema.lua`) to `apps/game/`
- [x] 1.2.5 Move `screens/`, `components/`, `ui/`, `libraries/`, `stories/`, `pics/` to `apps/game/`
- [x] 1.2.6 Delete `image_thread.lua` (replaced by React bridge)
- [x] 1.2.7 Keep `openspec/` at repository root
- [x] 1.2.8 Update any hardcoded paths in moved files

### 1.3 Nx Project Configuration

- [x] 1.3.1 Create `apps/backend/project.json` with build/serve/dev targets
- [x] 1.3.2 Create `apps/game/project.json` with build (love.js) and dev (native love) targets
- [x] 1.3.3 Create `apps/app/project.json` with Vite build/serve and Tauri targets
- [x] 1.3.4 Configure inter-project dependencies (app depends on game:build)
- [x] 1.3.5 Test `nx dev backend` runs the Hono server

### 1.4 React + Vite App Setup

- [x] 1.4.1 Generate React app in `apps/app/` using Nx generator
- [x] 1.4.2 Configure Vite with TypeScript, React Fast Refresh
- [x] 1.4.3 Create `apps/app/src/main.tsx` entry point
- [x] 1.4.4 Create `apps/app/src/App.tsx` with GameRunner integration
- [x] 1.4.5 Create `apps/app/public/` directory for static assets
- [x] 1.4.6 Test `nx serve app` runs development server

**Phase 1 Deliverable:** Monorepo structure works, `nx dev backend` runs, `love apps/game` runs natively

---

## Phase 2: Tauri Shell

*Desktop wrapper, Love.js compilation, bridge setup*

### 2.1 Tauri v2 Initialization

- [x] 2.1.1 Run `pnpm tauri init` in `apps/app/` directory
- [x] 2.1.2 Configure `apps/app/src-tauri/tauri.conf.json`:
  - Window: 1280x720, resizable, centered
  - CSP: Allow localhost:3000, wasm-unsafe-eval
  - Bundle resources: public/game/* (deferred until Love.js built)
- [x] 2.1.3 Create `apps/app/src-tauri/capabilities/default.json`:
  - `core:default`, `http:default`, `fs:default`
  - `fs:allow-app-write`, `fs:allow-app-read`
  - Remote URLs: `http://localhost:3000/*`
- [ ] 2.1.4 Create `apps/app/src-tauri/Info.plist` for macOS microphone permission
- [x] 2.1.5 Add Tauri icons in `apps/app/src-tauri/icons/` (generated by tauri init)
- [x] 2.1.6 Test `nx tauri:dev app` launches Tauri window

### 2.2 Love.js Build Pipeline

- [x] 2.2.1 Create `tools/love-builder/build.js` script:
  - Zip `apps/game/` → `game.love`
  - Run love.js compilation
  - Output to `apps/app/public/game/`
- [x] 2.2.2 Create `tools/love-builder/build.bat` for Windows
- [x] 2.2.3 Create custom `tools/love-builder/index.html.template` with postMessage bridge
- [x] 2.2.4 Add `build:game` script to root `package.json`
- [x] 2.2.5 Configure Nx to run game build before app build
- [x] 2.2.6 Test Love.js compilation produces working WASM

### 2.3 PostMessage Bridge (Lua Side)

- [x] 2.3.1 Create `apps/game/bridge.lua` module:
  - `bridge.init()` - Setup message listener
  - `bridge.requestImageGeneration(params, callback)` - Request via postMessage
  - `bridge.saveStory(filename, data, callback)` - Save via React
  - Native HTTP fallback for `love .` development
- [x] 2.3.2 Add `_G.postToReact` global for Love.js JavaScript injection
- [x] 2.3.3 Add `bridge.handleMessage(json)` for receiving React responses

**Phase 2 Deliverable:** Tauri app launches, Love.js game loads in iframe, bridge module ready

---

## Phase 3: Game Refactoring

*Remove threading, integrate bridge*

### 3.1 Editor Threading Removal

- [x] 3.1.1 Remove `love.thread.newThread("image_thread.lua")` from `editor.lua` (lines 1298, 1329)
- [x] 3.1.2 Remove channel polling from `editor.lua` update loop (lines 86-114)
- [x] 3.1.3 Replace `startImageGeneration()` to use `bridge.requestImageGeneration()`
- [x] 3.1.4 Replace `startCoverGeneration()` to use `bridge.requestImageGeneration()`
- [x] 3.1.5 Update `main.lua` to call `bridge.init()` on startup (web mode only)
- [x] 3.1.6 Test native mode still works with `love apps/game`

**Phase 3 Deliverable:** Game works without `image_thread.lua`, native mode still functional

---

## Phase 4: React Integration

*Connect React to game and backend*

### 4.1 React Components

- [x] 4.1.1 Create `apps/app/src/components/GameRunner.tsx`:
  - Full-screen iframe pointing to `/game/index.html`
  - `onGameReady` callback when game loads
  - `onGameMessage` callback for game requests
  - `sendToGame(message)` function for responses
- [x] 4.1.2 Create `apps/app/src/hooks/useGameBridge.ts`:
  - Message type definitions
  - Request/response handling
  - Pending request tracking with requestId

### 4.2 API Integration

- [x] 4.2.1 Create `apps/app/src/api/api.ts`:
  - `generateImage(description, question)` - POST to Hono backend
  - `saveStory(filename, data)` - Tauri fs API
  - `loadStory(filename)` - Tauri fs API
  - `listStories()` - Directory listing
- [x] 4.2.2 Configure Tauri HTTP plugin for backend calls
- [x] 4.2.3 Configure Tauri FS plugin for local storage
- [x] 4.2.4 Wire App.tsx to handle game messages and call api.ts

**Phase 4 Deliverable:** Full loop works: Game → React → Backend → React → Game

---

## Phase 5: Polish & Testing

*Scripts, testing, documentation*

### 5.1 Root Package Scripts

- [x] 5.1.1 Add `dev` script: Run backend + app in parallel
- [x] 5.1.2 Add `dev:backend` script: Run Hono server only
- [x] 5.1.3 Add `dev:app` script: Run Tauri dev mode
- [x] 5.1.4 Add `dev:game` script: Run native Love2D
- [x] 5.1.5 Add `build` script: Build all packages
- [x] 5.1.6 Add `build:app` script: Build Tauri distributable

### 5.2 Integration Testing

- [x] 5.2.1 Love.js game loads and runs in browser (play mode only)
- [x] 5.2.2 Fix Tauri app iframe loading (fixed with 100vw/100vh wrapper + display:block)
- [x] 5.2.3 Configure COOP/COEP headers for SharedArrayBuffer (vite.config.ts)
- [ ] 5.2.4 Test full flow: Game requests image → React calls API → Game receives image
- [ ] 5.2.5 Test story save/load via Tauri fs API
- [ ] 5.2.6 Test native Love2D mode with bridge fallback
- [ ] 5.2.7 Build and test Tauri distributable on Windows
- [ ] 5.2.8 Build and test Tauri distributable on macOS

### 5.3 Documentation

- [x] 5.3.1 Update root README.md with new project structure
- [x] 5.3.2 Document development workflow (DEVELOPMENT.md created)
- [ ] 5.3.3 Document build process for distributables

### 5.4 Web Mode Compatibility (Added)

- [x] 5.4.1 Disable Slab UI in web mode (requires LuaJIT bit/ffi modules)
- [x] 5.4.2 Make main.lua conditionally load editor (IS_WEB flag)
- [x] 5.4.3 Make ui/style.lua conditionally load Slab
- [x] 5.4.4 Web mode runs in play-only mode
- [ ] 5.4.5 (Future) Replace Slab with SUIT for web editor support

**Phase 5 Deliverable:** Production-ready builds for Windows + macOS

---

## Phase 6: Speech-to-Text (LOW PRIORITY)

*Voice input for answers - after core port complete*

### 6.1 Speech Integration

- [ ] 6.1.1 Create `apps/app/src/hooks/useSpeechToText.ts`:
  - Web Speech API integration
  - `startListening()` / `stopListening()` controls
  - Interim and final results handling
- [ ] 6.1.2 Add `bridge.requestSpeechToText(callback)` to bridge.lua
- [ ] 6.1.3 Add microphone button to game Play mode UI
- [ ] 6.1.4 Wire speech results to game via bridge
- [ ] 6.1.5 Test speech-to-text flow end-to-end

**Phase 6 Deliverable:** Voice answers work in Tauri app
